import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  appDir: true, // This enables the app directory in Next.js 13
  reactStrictMode: true,
};

export default nextConfig;
